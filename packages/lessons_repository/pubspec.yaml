name: lessons_repository
description: "Lessons repository."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.0.2
  json_annotation: ^4.9.0
  firestore_collection:
    path: ../firestore_collection

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  json_serializable: ^6.9.5
  build_runner: ^2.5.4

flutter: